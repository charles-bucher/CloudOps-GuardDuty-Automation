# Step 1: Generate a Terraform execution plan
terraform plan -out=tfplan

# Step 2: Apply the saved plan
terraform apply "tfplan"

# Notes:
# - tfplan stores the proposed infrastructure changes
# - Applying tfplan ensures predictable, reviewed deployments
# - This workflow is ideal for security-sensitive environments
